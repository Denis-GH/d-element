.confirmModal {
  padding: 4rem;
  aspect-ratio: 457 /180;
  border-radius: 2rem;

  &__title {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 6rem;
  }

  &__buttons {
    display: flex;
    justify-content: center;
    gap: 3rem;
  }
}
